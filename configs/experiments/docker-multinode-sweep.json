{
  "name": "docker-multinode-sweep",
  "description": "Multi-node scaling sweep across the 6-node Docker Slurm cluster",
  "steps": [
    {
      "name": "multinode-sweep",
      "arts_config": "docker/arts-docker.cfg",
      "launcher": "slurm",
      "nodes": "2,4,6",
      "debug": 0,
      "runs": 5,
      "perf": false,
      "profile": "profile-timing.cfg"
    }
  ]
}
