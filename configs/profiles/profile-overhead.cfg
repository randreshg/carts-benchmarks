# ARTS Counter Profile: FULL OVERHEAD ANALYSIS
# =============================================
#
# Captures comprehensive runtime overhead metrics at CLUSTER level.
# Use for detailed performance analysis and debugging.
#
# Usage:
#   carts build --arts --counters=3
#   carts build --arts --profile=profile-overhead.cfg

# EDT Lifecycle
TIME_EDT_EXEC=ONCE,CLUSTER
NUM_EDT_CREATE=ONCE,CLUSTER
NUM_EDT_ACQUIRE=ONCE,CLUSTER
NUM_EDT_FINISH=ONCE,CLUSTER

# DB Lifecycle
NUM_DB_CREATE=ONCE,CLUSTER

# Memory
BYTES_MEMORY_FOOTPRINT=ONCE,CLUSTER

# Remote Communication
BYTES_REMOTE_SENT=ONCE,CLUSTER
BYTES_REMOTE_RECEIVED=ONCE,CLUSTER

# Timing Counters
TIME_INIT=ONCE,CLUSTER,MASTER
TIME_TOTAL=ONCE,CLUSTER,MASTER

# Acquire-Mode Override Effectiveness
NUM_DB_ACQUIRE_READ=ONCE,CLUSTER
NUM_DB_ACQUIRE_WRITE=ONCE,CLUSTER
NUM_OWNER_UPDATE_SAVED=ONCE,CLUSTER
NUM_OWNER_UPDATE_PERFORMED=ONCE,CLUSTER

# ArtsID Tracking (disabled by default, enable for per-task attribution)
OBJ_NUM_EDT=OFF
OBJ_NUM_DB=OFF
OBJ_TRACE_EDT=OFF
OBJ_TRACE_DB=OFF
